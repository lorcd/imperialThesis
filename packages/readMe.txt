This txt file lays out how to ensure same package versions are used.
Use in conjunction with virtual environment.


Created requirements.txt using: 
pip3 freeze > requirements.txt

Enter virtual environment (IMPORTANT; for example: source activateVirtualEnv), and Install specified versions of packages using:
pip3 install -r requirements.txt
